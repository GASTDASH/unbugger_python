class Product():
    name: str
    count: int
    type_of_count: str
    deliver_id: int
    last_delivery: str

    # def __init__(self, name, count, type_of_count, deliver_id, last_delivery):
    #     self.name = name
    #     self.count = count
    #     self.type_of_count = type_of_count
    #     self.deliver_id = deliver_id
    #     self.last_delivery = last_delivery